#include <stdio.h>

/*
 * main - print the alphabit in lowercase followed by new line
 * Return: Always 0 (Success) 
 */

int main(void){

	char m;
	for (m = 'a';m <='z'; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);


}
